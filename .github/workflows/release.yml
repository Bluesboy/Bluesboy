name: Release and deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

# Pages accepts one deployment at a time, and a half-finished one should
# not be cancelled.
concurrency:
  group: pages
  cancel-in-progress: false

permissions: {}

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      pages: write
    outputs:
      tag: ${{ steps.plan.outputs.tag }}
    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0
    - uses: actions/setup-python@v7
      with:
        python-version: '3.13'
    - uses: actions/setup-go@v7
      with:
        go-version: '1.25'
        # Only used to build actionlint; there is no go.mod to cache against.
        cache: false
    - uses: peaceiris/actions-hugo@v3
      with:
        hugo-version: '0.165.0'
        extended: true
    - uses: typst-community/setup-typst@v5
      with:
        typst-version: '0.15.1'
    # Installs the cog binary only; version/plan and version/release drive it
    # from the Makefile. Commit messages are validated in CI, where a
    # malformed one turns the check red instead of blocking a deployment.
    # install-only also skips the action's global git config, so the release
    # identity set below stays in force.
    - uses: cocogitto/cocogitto-action@v4.2.0
      with:
        install-only: true

    - id: tools
      run: echo "key=$(make -s deps/versions)" >> "$GITHUB_OUTPUT"
    - uses: actions/cache@v6
      with:
        path: |
          ~/.cargo/bin/typstyle
          ~/go/bin/actionlint
        key: lint-tools-${{ runner.os }}-${{ steps.tools.outputs.key }}
    - name: Install validators
      run: make deps/install

    # Predict the tag without creating it, so the build runs first and a
    # failed build cannot leave a tag behind.
    - id: plan
      run: echo "tag=$(make -s version/plan)" >> "$GITHUB_OUTPUT"
    - name: Build and test
      env:
        TAG: ${{ steps.plan.outputs.tag }}
      run: make ci ${TAG:+VERSION=$TAG}

    - name: Configure release identity
      if: steps.plan.outputs.tag != ''
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
    - name: Create version tag
      if: steps.plan.outputs.tag != ''
      run: make --no-print-directory version/release
    - name: Publish GitHub Release
      if: steps.plan.outputs.tag != ''
      env:
        GH_TOKEN: ${{ github.token }}
      run: make release VERSION=${{ steps.plan.outputs.tag }}

    # The site deploys on every push to master; only the PDFs wait for a bump.
    - uses: actions/configure-pages@v6
    - uses: actions/upload-pages-artifact@v5
      with:
        path: build/site

  deploy:
    needs: release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
    - name: Deploy GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v5
